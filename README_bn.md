# Sheikh AI

[English](README.md) | [中文](README_zh.md) | বাংলা | [অফিশিয়াল সাইট](https://sheikhAI.com) | [ডকুমেন্টেশন](https://docs.sheikhAI.com)

[![GitHub stars](https://img.shields.io/github/stars/likhonsexikh/sheikh-ai?style=social)](https://github.com/likhonsexikh/sheikh-ai/stargazers)
&ensp;
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Sheikh AI হল একটি সাধারণ উদ্দেশ্যের AI এজেন্ট সিস্টেম যা স্যান্ডবক্স পরিবেশে বিভিন্ন টুল এবং অপারেশন চালানোর সুবিধা প্রদান করে।

Sheikh AI এর সাথে আপনার নিজস্ব এজেন্ট উপভোগ করুন!

❤️ Sheikh AI পছন্দ হয়েছে? স্টার 🌟 দিয়ে উন্নয়নে সহায়তা করুন!

🚀 [ডেমো দেখুন](https://sheikhAI.com)

## ডেমো

### মৌলিক বৈশিষ্ট্য

https://github.com/user-attachments/assets/37060a09-c647-4bcb-920c-959f7fa73ebe

### ব্রাউজার ব্যবহার

* কাজ: সর্বশেষ LLM গবেষণাপত্র

<https://github.com/user-attachments/assets/4e35bc4d-024a-4617-8def-a537a94bd285>

### কোড ব্যবহার

* কাজ: জটিল Python উদাহরণ লেখা

<https://github.com/user-attachments/assets/765ea387-bb1c-4dc2-b03e-716698feef77>


## মূল বৈশিষ্ট্য

 * ডিপ্লয়মেন্ট: ন্যূনতম ডিপ্লয়মেন্টের জন্য শুধুমাত্র একটি LLM সার্ভিস প্রয়োজন, অন্য কোনো বাহ্যিক সার্ভিসের উপর নির্ভরতা নেই।
 * টুলস: টার্মিনাল, ব্রাউজার, ফাইল, ওয়েব সার্চ এবং মেসেজিং টুলস সমর্থন করে রিয়েল-টাইম দেখা এবং টেকওভার ক্ষমতা সহ, বাহ্যিক MCP টুল ইন্টিগ্রেশন সমর্থন করে।
 * স্যান্ডবক্স: প্রতিটি টাস্কের জন্য আলাদা স্যান্ডবক্স বরাদ্দ করা হয় যা লোকাল Docker পরিবেশে চলে।
 * টাস্ক সেশন: MongoDB/Redis এর মাধ্যমে সেশন ইতিহাস পরিচালনা করা হয়, ব্যাকগ্রাউন্ড টাস্ক সমর্থন করে।
 * কথোপকথন: থামানো এবং বাধা দেওয়া সমর্থন করে, ফাইল আপলোড এবং ডাউনলোড।
 * বহুভাষিক: চীনা এবং ইংরেজি উভয়ই সমর্থন করে।
 * প্রমাণীকরণ: ব্যবহারকারী লগইন এবং প্রমাণীকরণ।

## উন্নয়ন রোডম্যাপ

 * টুলস: Deploy & Expose এর জন্য সমর্থন।
 * স্যান্ডবক্স: মোবাইল এবং Windows কম্পিউটার অ্যাক্সেসের জন্য সমর্থন।
 * ডিপ্লয়মেন্ট: K8s এবং Docker Swarm মাল্টি-ক্লাস্টার ডিপ্লয়মেন্টের জন্য সমর্থন।

### সামগ্রিক ডিজাইন

![Image](https://github.com/user-attachments/assets/69775011-1eb7-452f-adaf-cd6603a4dde5)

**যখন একজন ব্যবহারকারী কথোপকথন শুরু করে:**

1. Web সার্ভারে একটি এজেন্ট তৈরি করার জন্য একটি অনুরোধ পাঠায়, যা `/var/run/docker.sock` এর মাধ্যমে একটি স্যান্ডবক্স তৈরি করে এবং একটি সেশন ID রিটার্ন করে।
2. স্যান্ডবক্স হল একটি Ubuntu Docker পরিবেশ যা Chrome ব্রাউজার এবং File/Shell এর মতো টুলসের জন্য API সার্ভিস চালু করে।
3. Web সেশন ID-তে ব্যবহারকারীর বার্তা পাঠায়, এবং যখন সার্ভার ব্যবহারকারীর বার্তা পায়, তখন এটি প্রসেসিংয়ের জন্য PlanAct এজেন্টে ফরওয়ার্ড করে।
4. প্রসেসিংয়ের সময়, PlanAct এজেন্ট টাস্ক সম্পন্ন করতে প্রাসঙ্গিক টুলস কল করে।
5. এজেন্ট প্রসেসিংয়ের সময় উৎপন্ন সমস্ত ইভেন্ট SSE এর মাধ্যমে Web-এ ফিরে পাঠানো হয়।

**যখন ব্যবহারকারীরা টুলস ব্রাউজ করে:**

- ব্রাউজার:
    1. স্যান্ডবক্সের হেডলেস ব্রাউজার xvfb এবং x11vnc এর মাধ্যমে একটি VNC সার্ভিস চালু করে, এবং websockify এর মাধ্যমে VNC কে websocket এ রূপান্তরিত করে।
    2. Web-এর NoVNC কম্পোনেন্ট সার্ভারের Websocket Forward এর মাধ্যমে স্যান্ডবক্সের সাথে সংযুক্ত হয়, ব্রাউজার দেখা সক্ষম করে।
- অন্যান্য টুলস: অন্যান্য টুলস একই নীতিতে কাজ করে।

## পরিবেশের প্রয়োজনীয়তা

এই প্রকল্পটি মূলত ডেভেলপমেন্ট এবং ডিপ্লয়মেন্টের জন্য Docker এর উপর নির্ভর করে, যার জন্য Docker এর তুলনামূলকভাবে নতুন সংস্করণ প্রয়োজন:
- Docker 20.10+
- Docker Compose

মডেল ক্ষমতা প্রয়োজনীয়তা:
- OpenAI ইন্টারফেসের সাথে সামঞ্জস্যপূর্ণ
- FunctionCall এর জন্য সমর্থন
- Json Format আউটপুটের জন্য সমর্থন

Deepseek এবং GPT মডেল সুপারিশ করা হয়।

## ডিপ্লয়মেন্ট গাইড

ডিপ্লয়মেন্টের জন্য Docker Compose সুপারিশ করা হয়:

```yaml
services:
  frontend:
    image: likhonsexikh/sheikh-ai-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - sheikh-ai-network
    environment:
      - BACKEND_URL=http://backend:8000

  backend:
    image: likhonsexikh/sheikh-ai-backend
    depends_on:
      - sandbox
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      #- ./mcp.json:/etc/mcp.json # MCP সার্ভার ডিরেক্টরি মাউন্ট করুন
    networks:
      - sheikh-ai-network
    environment:
      # OpenAI API বেস URL
      - API_BASE=https://api.openai.com/v1
      # OpenAI API কী, আপনার নিজস্ব দিয়ে প্রতিস্থাপন করুন
      - API_KEY=sk-xxxx
      # LLM মডেল নাম
      - MODEL_NAME=gpt-4o
      # LLM তাপমাত্রা প্যারামিটার, এলোমেলোতা নিয়ন্ত্রণ করে
      - TEMPERATURE=0.7
      # LLM প্রতিক্রিয়ার জন্য সর্বোচ্চ টোকেন
      - MAX_TOKENS=2000

      # MongoDB সংযোগ URI
      #- MONGODB_URI=mongodb://mongodb:27017
      # MongoDB ডাটাবেস নাম
      #- MONGODB_DATABASE=sheikh_ai
      # MongoDB ব্যবহারকারীর নাম (ঐচ্ছিক)
      #- MONGODB_USERNAME=
      # MongoDB পাসওয়ার্ড (ঐচ্ছিক)
      #- MONGODB_PASSWORD=

      # Redis সার্ভার হোস্টনাম
      #- REDIS_HOST=redis
      # Redis সার্ভার পোর্ট
      #- REDIS_PORT=6379
      # Redis ডাটাবেস নম্বর
      #- REDIS_DB=0
      # Redis পাসওয়ার্ড (ঐচ্ছিক)
      #- REDIS_PASSWORD=

      # স্যান্ডবক্স সার্ভার ঠিকানা (ঐচ্ছিক)
      #- SANDBOX_ADDRESS=
      # স্যান্ডবক্সের জন্য ব্যবহৃত Docker ইমেজ
      - SANDBOX_IMAGE=likhonsexikh/sheikh-ai-sandbox
      # স্যান্ডবক্স কন্টেইনার নামের উপসর্গ
      - SANDBOX_NAME_PREFIX=sandbox
      # মিনিটে স্যান্ডবক্স কন্টেইনারের জীবনকাল
      - SANDBOX_TTL_MINUTES=30
      # স্যান্ডবক্স কন্টেইনারের জন্য Docker নেটওয়ার্ক
      - SANDBOX_NETWORK=sheikh-ai-network

      # সার্চ ইঞ্জিন কনফিগারেশন
      # অপশন: baidu, google, bing
      - SEARCH_PROVIDER=bing

      # প্রমাণীকরণ কনফিগারেশন
      # অপশন: password, none, local
      - AUTH_PROVIDER=password

      # পাসওয়ার্ড প্রমাণীকরণ কনফিগারেশন
      - PASSWORD_SALT=
      - PASSWORD_HASH_ROUNDS=10

      # JWT কনফিগারেশন
      - JWT_SECRET_KEY=your-secret-key-here
      - JWT_ALGORITHM=HS256
      - JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
      - JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

      # লগ লেভেল
      - LOG_LEVEL=INFO

  sandbox:
    image: likhonsexikh/sheikh-ai-sandbox
    command: /bin/sh -c "exit 0"
    restart: "no"
    networks:
      - sheikh-ai-network

  mongodb:
    image: mongo:7.0
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped
    networks:
      - sheikh-ai-network

  redis:
    image: redis:7.0
    restart: unless-stopped
    networks:
      - sheikh-ai-network

volumes:
  mongodb_data:
    name: sheikh-ai-mongodb-data

networks:
  sheikh-ai-network:
    name: sheikh-ai-network
    driver: bridge
```

`docker-compose.yml` ফাইল হিসেবে সংরক্ষণ করুন এবং চালান:

```shell
docker compose up -d
```

> নোট: আপনি যদি `sandbox-1 exited with code 0` দেখেন, এটি স্বাভাবিক, কারণ এটি নিশ্চিত করে যে স্যান্ডবক্স ইমেজ স্থানীয়ভাবে সফলভাবে টেনে নেওয়া হয়েছে।

আপনার ব্রাউজার খুলুন এবং Sheikh AI অ্যাক্সেস করতে <http://localhost:5173> ভিজিট করুন।

## ডেভেলপমেন্ট গাইড

### প্রকল্প কাঠামো

এই প্রকল্পটি তিনটি স্বতন্ত্র সাব-প্রকল্প নিয়ে গঠিত:

* `frontend`: Sheikh AI ফ্রন্টএন্ড
* `backend`: Sheikh AI ব্যাকএন্ড
* `sandbox`: Sheikh AI স্যান্ডবক্স

### পরিবেশ সেটআপ

1. প্রকল্প ডাউনলোড করুন:
```bash
git clone https://github.com/likhonsexikh/sheikh-ai.git
cd sheikh-ai
```

2. কনফিগারেশন ফাইল কপি করুন:
```bash
cp .env.example .env
```

3. কনফিগারেশন ফাইল সংশোধন করুন:

```env
# মডেল প্রদানকারী কনফিগারেশন
API_KEY=
API_BASE=http://mockserver:8090/v1

# মডেল কনফিগারেশন
MODEL_NAME=deepseek-chat
TEMPERATURE=0.7
MAX_TOKENS=2000

# MongoDB কনফিগারেশন
#MONGODB_URI=mongodb://mongodb:27017
#MONGODB_DATABASE=sheikh_ai
#MONGODB_USERNAME=
#MONGODB_PASSWORD=

# Redis কনফিগারেশন
#REDIS_HOST=redis
#REDIS_PORT=6379
#REDIS_DB=0
#REDIS_PASSWORD=

# স্যান্ডবক্স কনফিগারেশন
#SANDBOX_ADDRESS=
SANDBOX_IMAGE=likhonsexikh/sheikh-ai-sandbox
SANDBOX_NAME_PREFIX=sandbox
SANDBOX_TTL_MINUTES=30
SANDBOX_NETWORK=sheikh-ai-network

# সার্চ ইঞ্জিন কনফিগারেশন
SEARCH_PROVIDER=bing

# প্রমাণীকরণ কনফিগারেশন
AUTH_PROVIDER=password

# পাসওয়ার্ড প্রমাণীকরণ
PASSWORD_SALT=
PASSWORD_HASH_ROUNDS=10

# JWT কনফিগারেশন
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# লগ কনফিগারেশন
LOG_LEVEL=INFO
```

### ডেভেলপমেন্ট এবং ডিবাগিং

1. ডিবাগ মোডে চালান:
```bash
# docker compose -f docker-compose-development.yaml up এর সমতুল্য
./dev.sh up
```

সমস্ত সার্ভিস রিলোড মোডে চলবে, এবং কোড পরিবর্তনগুলি স্বয়ংক্রিয়ভাবে রিলোড হবে। উন্মুক্ত পোর্টগুলি নিম্নরূপ:
- 5173: Web ফ্রন্টএন্ড পোর্ট
- 8000: সার্ভার API সার্ভিস পোর্ট
- 8080: স্যান্ডবক্স API সার্ভিস পোর্ট
- 5900: স্যান্ডবক্স VNC পোর্ট
- 9222: স্যান্ডবক্স Chrome ব্রাউজার CDP পোর্ট

> *নোট: ডিবাগ মোডে, বিশ্বব্যাপী শুধুমাত্র একটি স্যান্ডবক্স চালু হবে*

2. যখন নির্ভরতা পরিবর্তন হয় (requirements.txt বা package.json), পরিষ্কার করুন এবং পুনর্নির্মাণ করুন:
```bash
# সমস্ত সম্পর্কিত সংস্থান পরিষ্কার করুন
./dev.sh down -v

# ইমেজ পুনর্নির্মাণ করুন
./dev.sh build

# ডিবাগ মোডে চালান
./dev.sh up
```

### ইমেজ প্রকাশনা

```bash
export IMAGE_REGISTRY=likhonsexikh
export IMAGE_TAG=latest

# ইমেজ তৈরি করুন
./run build

# সংশ্লিষ্ট ইমেজ রিপোজিটরিতে পুশ করুন
./run push
```

## ক্রেডিট

মূল [AI Manus](https://github.com/simpleyyt/ai-manus) প্রকল্পের উপর ভিত্তি করে।

## লাইসেন্স

MIT License - বিস্তারিত জানতে [LICENSE](LICENSE) ফাইল দেখুন।

## ⭐️ স্টার হিস্ট্রি

[![Star History Chart](https://api.star-history.com/svg?repos=likhonsexikh/sheikh-ai&type=Date)](https://www.star-history.com/#likhonsexikh/sheikh-ai&Date)

## সহায়তা

সমস্যা বা প্রশ্ন থাকলে, অনুগ্রহ করে [GitHub Issues](https://github.com/likhonsexikh/sheikh-ai/issues) তে একটি ইস্যু খুলুন।
